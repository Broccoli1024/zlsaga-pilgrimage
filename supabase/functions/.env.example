# Edge Functions only. Never expose these through VITE_ variables.
# SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY are provided
# by the Supabase runtime; use its local values when serving locally.
# Additional secrets required by fetch-form-responses:
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_KEY=
GOOGLE_SHEET_ID=
DEEPL_API_KEY=
